# ─────────────────────────────────────────────────────────────
# CHECKOUT ROUTES
# Create each product in Stripe → Payment Link → set the
# after-payment redirect to https://abhomesolutions.pro/guide-download
# then replace the REPLACE_… URL below with the real link.
# ─────────────────────────────────────────────────────────────

# HomeGuard™ Guide — $7.99 one-time
/buy-homeguard   https://buy.stripe.com/REPLACE_HOMEGUARD_LINK   302!

# Contractor Clarity™ Guide — $7.99 one-time
/buy-clarity     https://buy.stripe.com/REPLACE_CLARITY_LINK     302!

# The Interior Guide — $7.99 one-time
/buy-interior    https://buy.stripe.com/REPLACE_INTERIOR_LINK    302!

# Any 2 guides — $13.50
# NOTE: add a dropdown custom field in Stripe so the buyer picks which two.
/buy-bundle-2    https://buy.stripe.com/REPLACE_BUNDLE2_LINK     302!

# All 3 guides — $18
/buy-bundle-3    https://buy.stripe.com/REPLACE_BUNDLE3_LINK     302!

# Legacy /buy → HomeGuard guide (keeps old links and receipts working)
/buy             /buy-homeguard                                  301!

# ─────────────────────────────────────────────────────────────
# RETIRED PRO TIER — 301 so nothing 404s
# ─────────────────────────────────────────────────────────────
/pro             /homeguard#buy                                  301!
/pro-welcome     /homeguard#buy                                  301!
/buy-pro         /homeguard#buy                                  301!

# ─────────────────────────────────────────────────────────────
# BLOG
# Posts live as <slug>.html at the repo root but are linked as
# /blog/<slug>. This rewrite serves the file without changing the URL.
# ─────────────────────────────────────────────────────────────
/blog/*          /:splat.html                                    200
